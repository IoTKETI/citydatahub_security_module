<!DOCTYPE html>
<html lang="ko">
<head>
    <title>Smartcity Hub App</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <link rel="stylesheet" href="/stylesheets/login.css">
    <link rel="stylesheet" href="/stylesheets/dhsec.css">
    <script type="text/javascript" src="/js/jquery-3.3.1.min.js"></script>
    <script type="text/javascript" src="/js/apps.js"></script>
</head>
<body>
    <div class="join__wrap">
        <div class="join__inner">
            <input type="hidden" id="appNameOrigin" value="<%= appInfo.applicationName %>">
            <input type="hidden" id="appUriOrigin" value="<%= appInfo.redirectUri %>">
            <input type="hidden" id="cType" value="<%= isSysApp %>">
            <header class="join__header">
                <h1 class="join__title"><span class="join__title--top">CITY HUB</span>어플리케이션 정보 수정</h1>
            </header>
            <main class="join__main">
                <div id="inProgress">
                    <fieldset>
                        <div class="join__row">
                            <label for="applicationName" class="join__label">어플리케이션 이름</label>
                            <input id="applicationName" class="join__input input" type="text" name='applicationName' value='<%= appInfo.applicationName %>' onchange="checkAppName();" required>
                            <div class="warning_msg" id="applicationNameMsg" style="display:none"></div>
                        </div>
                        <div class="join__row">
                            <label for="redirectUri" class="join__label">리다이렉션 URI</label>
                            <input id="redirectUri" class="join__input input" type="text" name='redirectUri' value='<%= appInfo.redirectUri %>' onchange="checkUrl();" required>
                            <div class="warning_msg" id="redirectUriMsg" style="display:none"></div>
                        </div>
                        <div class="join__bottom">
                            <a class="button button__primary" type="button" style="cursor: pointer;" onclick="changeAppInfo();">수정하기</a>
                            <a class="button button__primary" type="button" style="background-color: #e52b50; cursor: pointer;" onclick="history.go(-1)">돌아가기</a>
                        </div>
                    </fieldset>
                </div>
                <div class="footer">
                    <h2 class="footer__logo"><span class="hidden">City Hub</span></h2>
                    <ul class="mark__list">
                        <li class="mark__item"><span class="hidden">국토교통부</span></li>
                        <li class="mark__item"><span class="hidden">KAIA</span></li>
                        <li class="mark__item"><span class="hidden">KETI</span></li>
                    </ul>
                    <p class="footer__copyright">Copyright©SmartCity Datahub. All rights reserved.</p>
                </div>
            </main>
        </div>
    </div>
</body>
</html>